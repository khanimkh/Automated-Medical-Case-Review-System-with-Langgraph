version: '3.8'
services:
  medical_case_review:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medical_case_review
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
